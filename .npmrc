# Disable opencollective postinstall scripts
# This file helps prevent opencollective-postinstall from running

# Skip Puppeteer Chromium download during build to speed up installation
# Chromium will be downloaded at runtime or use system Chromium
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_SKIP_DOWNLOAD=true
