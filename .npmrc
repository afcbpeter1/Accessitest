# Disable opencollective postinstall scripts (they're just donation prompts)
# These scripts use 'find' commands that can fail if packages aren't fully installed
# The DISABLE_OPENCOLLECTIVE env var is set in render.yaml build command

# Ensure npm doesn't fail on optional dependencies
optional=true

# Increase timeout for slow installs
fetch-timeout=300000
fetch-retries=5

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=false

# Ignore scripts for problematic packages (but this is global, so we use env var instead)
# ignore-scripts=true  # Don't use this - it breaks native modules

